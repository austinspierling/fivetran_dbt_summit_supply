name: 'fivetran_dbt_summit_supply'
version: '1.0.0'

profile: 'fivetran_dbt_summit_supply'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  fivetran_dbt_summit_supply:
    staging:
      +materialized: view
    marts:
      +materialized: table